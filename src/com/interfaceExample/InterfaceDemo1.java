package com.interfaceExample;

public interface InterfaceDemo1 {
	
		// any service requirement specification by default are called as interfaces.
		// all the abstract methods in interfaces must be implemented.
		// if the method is not implemented we should declare the class as abstract.
		// methods in interfaces are always public and abstract by default.
		// no body or declaration of the methods on interfaces.
		// all the attributes declared in the interface must be public, static and final
	
	
	public void run();
	
	public void walk();
	
}
